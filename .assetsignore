# Repo/meta files that shouldn't be served publicly.
# (Pages already excludes functions/, wrangler.toml, node_modules, and .git.)
*.md
schema.sql
package.json
package-lock.json
.claude
.dev.vars
.assetsignore
